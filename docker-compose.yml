services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: legis
      POSTGRES_USER: legis
      POSTGRES_PASSWORD: legis_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U legis"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
